import './bootstrap';

// Bootstrap JS (dropdown, modal, offcanvas, tooltip, etc)
import * as bootstrap from 'bootstrap';

// Expose bootstrap for legacy inline usage (optional)
window.bootstrap = bootstrap;

//
